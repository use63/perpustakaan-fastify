// appConfig.js
const appConfig = {
	server: {
		host: "0.0.0.0",
		port: 8080,
	},
	database: {
		// your database configuration
	},
	// other application configurations
};

export default appConfig;
